#include<stdio.h>
void main()
{
    int side,area,per;
    printf(" enter the side : ");
    scanf("%d",&side);
    area = side*side;
    per = 4*side;
    printf(" the area of the square is %d the perimeter of the square is %d",area,per);
}
